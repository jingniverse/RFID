# 📂 방문요양 급여제공기록지 & RFID 스마트 홈케어 프로젝트 명세서 (PROJECT.md)

이 문서는 본 프로젝트의 아키텍처, 데이터 모델, 빌드 절차 및 주의 사항을 기록하여 개발자(및 AI 페어 프로그래머)가 인수인계 과정 없이 연속적으로 작업을 수행할 수 있도록 돕는 핵심 가이드라인입니다.

---

## 1. 프로젝트 개요 (Overview)
*   **목적**: 방문요양보호사들이 매일 작성하는 급여제공기록지를 편리하게 전산 입력하고, 공단 전송용 RFID 앱 화면과 인쇄용 수기 기록지 두 가지 형태에 맞춰 데이터를 양방향으로 렌더링 및 출력(인쇄)해 주는 통합 프로그램입니다.
*   **기술 스택**: 순수 HTML, CSS, Vanilla JS (ES6) 기반으로 웹 애플리케이션을 구현하며, 최종 배포를 위해 **Electron**을 활용해 무설치 1파일 포터블 실행 파일(`.exe`)로 패키징합니다.

---

## 2. 디렉토리 구조 및 핵심 파일 역할

```text
RFID/
├── dist/                          # 최종 패키징 배포본 (.exe) 보관 폴더
├── index.html                     # 프로그램 진입점 (버전 전환 허브 메인 화면)
├── main.js                        # Electron 메인 프로세스 (로컬 서버 구동 및 파일 I/O 제어)
├── package.json                   # 프로젝트 의존성 및 빌드 메타데이터
├── recipients.js                  # 전체 통합 수급자 템플릿 데이터 소스 파일
├── 통합_사용_설명서.html            # 패치노트 및 기능 사용 매뉴얼
│
├── RFID_APP/                      # 📱 RFID APP형 시뮬레이터 버전
│   ├── [RFID APP형].html          # APP 메인 렌더러 HTML
│   ├── css/rfid_style.css         # APP 전용 스타일 (주/월/필요시 색상 등)
│   └── js/
│       ├── recipients.js          # APP 전용 폴백 데이터
│       └── rfid_script.js         # APP 코어 기능 구현 (3종 비정기 Setter 및 라디오 연동)
│
└── 수기 급여제공기록지/             # 📝 인쇄용 수기 기록지 버전
    ├── [수기급여제공기록지].html    # 수기 메인 렌더러 및 인쇄용 뷰
    ├── [관리자] 수급자 등록 및 변경하기.html  # 수급자 및 요양보호사 템플릿 관리자 대시보드 (★공통)
    ├── css/style.css              # 수기 인쇄 가이드라인 스타일
    └── js/
        ├── recipients.js          # 수기 전용 폴백 데이터
        └── script.js              # 수기 코어 기능 (일일 서비스 전용 필터링 로직 포함)
```

---

## 3. 핵심 데이터 흐름 & 동기화 메커니즘
1.  **데이터 소스 (`recipients.js`)**:
    *   모든 수급자의 템플릿 설정(기본 시간, 체크박스 상태 등)은 JS 객체 배열(`INITIAL_RECIPIENTS`)로 선언되어 있습니다.
    *   포터블 앱에서 수급자를 추가/수정하여 저장하면, Electron 메인 프로세스의 `/api/save-recipients` API가 활성화되어 루트 폴더 및 하위 폴더의 `recipients.js` 소스 파일을 실시간으로 다시 덮어써서 영구 반영합니다.
2.  **안전 로컬 로드 패치 (Robust Fallback)**:
    *   사용자가 `.exe` 앱을 실행하지 않고 브라우저로 직접 HTML을 더블클릭(`file://`)하여 열더라도 수급자 정보가 100% 뜨도록 설계되었습니다.
    *   `INITIAL_RECIPIENTS` 변수가 정상 로드되지 않을 경우, 브라우저 캐시(`localStorage.rfid_recipients`)를 탐색하여 최신 데이터를 복원하고, 이것마저 비어있을 경우 내장된 기본 더미 데이터를 강제 기입해 오동작을 원천 차단합니다.
3.  **비정기 서비스 3종 분류 규칙**:
    *   **주간(Weekly)**: 옅은 핑크색 배경 / 빨간색 테두리 (`weekly-checked` / `weekly-active`)
    *   **월간(Monthly)**: 연한 초록색 배경 / 초록색 테두리 (`monthly-checked` / `monthly-active`)
    *   **필요시(As-Needed)**: 연한 보라색 배경 / 보라색 테두리 (`as-needed-checked` / `as-needed-active`)
    *   *동작*: 관리자 화면 상단의 3종 모드 스위치는 **라디오 단추처럼 오직 단 1개만 선택 가능**하며, 켜진 모드에 맞춰 아래 서브 항목을 클릭 시 실시간으로 해당 컬러 테마로 채워집니다.

---

## 4. ⚠️ 에디터 파일 락(Lock) 충돌 우회 빌드 공식
VS Code나 다른 텍스트 에디터가 파일을 열어 쥐고 있는 상태에서 `electron-builder`를 구동하면 권한 오류(File Lock)가 발생하며 패키징 빌드가 터집니다. 이를 완벽하게 피하기 위해 **임시 폴더 우회 빌드 방식**을 표준으로 채택하고 있습니다.

### 🛠️ 빌드 절차
1.  `package.json`의 `"build.directories.output"` 설정을 시스템 임시 폴더(`C:/Users/ziziz/AppData/Local/Temp/RFID-build`)로 우회 변경합니다.
2.  `npm run build` 명령을 실행합니다.
3.  빌드가 완료되면 생성된 `dist/RFID_v[버전].exe` 파일을 프로젝트의 실제 `dist/` 폴더로 복사합니다.
4.  임시 폴더를 삭제하고 `package.json` 설정을 원래대로 복구합니다.
*   *팁*: 이 귀찮은 복사/복원 단계를 편리하게 수행해 주는 자동화 스크립트(`update_package_json.js`, `finalize_build.js`)가 준비되어 있습니다.

## 5. ⚠️ AI 개발자 필수 준수 사항 (Mandatory Rules for AI)
> [!IMPORTANT]
> **버전 자동 최신화 의무**
> 새로운 기능 추가 또는 버그 수정이 발생할 경우, **사용자가 버전을 언급하지 않더라도 다음 사항을 스스로 판단하여 무조건 동시에 갱신**해야 합니다.
> 1. `package.json` 의 `"version"` 값 올리기.
> 2. `index.html` 내의 버전 표시 텍스트 및 `CURRENT_VERSION` 변수 올리기.
> 3. `통합_사용_설명서.html` 에 새로운 릴리즈 날짜 및 구체적인 패치 노트를 추가하고 최상단 버전을 올리기.
> 4. 본 `PROJECT.md` 파일 하단의 **5. 릴리즈 이력** 섹션에 신규 버전을 추가하고 변경 사항 요약을 반드시 최신화하기.

---

## 6. 릴리즈 이력 (Release History)
*   **v1.4.0 (2026.07.06) - 현 버전**
    *   가족요양(60분/90분) 서비스 시간 자동 계산 연동 및 수급자별 템플릿 저장 기능 구현.
    *   수급자 등록/변경 화면 레이아웃 최적화 (가로 병렬 컬럼 배치로 수직 중앙 수평 얼라인 조율).
    *   텍스트 인풋, 셀렉트 박스, 체크박스 테두리 상자 높이를 38px(border-box)로 강제 통일하여 정돈된 화면 설계.
    *   수급자 목록 테이블 및 메인 화면 수급자 검색 결과 리스트에 기존 '치매' 배지 대신 '가족' 배지 표기.
    *   수기 급여제공기록지 수급자 목록 드로어 너비를 330px에서 370px로 확장하여 공간 부족 해결.

*   **v1.3.3 (2026.06.28)**
    *   주간/월간(초록)/필요시(보라) 비정기 3종 데이터를 완전 분리 및 저장 구조 이식.
    *   수기 기록지 출력 시 주/월/필요시 비정기 설정 항목은 자동으로 필터링되어 출력 제외.
    *   로컬 브라우저 기동 시 수급자 목록 100% 로딩 보장 철통 패치 도입.
    *   관리자 화면 상단 3종 모드 상호 배타적 단일 선택 제어(라디오 단추 기능) 주입.


